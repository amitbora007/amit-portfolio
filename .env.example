# Server Configuration
PORT=5000
FRONTEND_URL=

SMTP_HOST=smtp.resend.com
SMTP_PORT=
SMTP_USER=resend
SMTP_PASS=
# Sender address shown in "From" field — must be verified in Resend dashboard
# For testing (no domain needed): onboarding@resend.dev
# For production: noreply@amitbora.dev (after verifying domain in Resend)
SMTP_FROM=

# Contact Receiver (Destination email for messages)
CONTACT_RECEIVER=
